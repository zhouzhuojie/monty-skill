[project]
name = "monty-skill"
version = "0.1.0"
description = "Run Python in a secure sandbox"
requires-python = ">=3.10"
dependencies = [
    "pydantic-monty",
]

[project.optional-dependencies]
dev = ["pytest"]

[tool.uv]
package = false
